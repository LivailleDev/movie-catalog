# Copy to .env.local and add your free TMDB API key (v3) to enable live data.
# Get one at https://www.themoviedb.org/settings/api
# Without a key, the app runs in demo mode with sample movies.
VITE_TMDB_KEY=
